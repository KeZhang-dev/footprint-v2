# Copy this file to .env (gitignored) before running `docker compose up`.

# Signing key for JWT auth tokens issued by the API container.
# Generate one locally, e.g.: openssl rand -base64 48
JWT_KEY=
